{
    "name": "TweedTrading",
    "version": "1.0.0",
    "description": "Cool website template",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "build2": "mkdir -p publicttb && (test -d publicttb && echo 'Directory already exists' || (cp index.html publicttb && cp styles.css publicttb && cp server.js publicttb))",
        "build": "mkdir -p public && cp index.html styles.css server.js public/"
    },
    "dependencies": {
        "emailjs": "^4.0.2",
        "express": "^4.17.1"
    }
}
