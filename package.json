{
    "name": "TweedTrading",
    "version": "1.0.0",
    "description": "Cool website template",
    "main": "server.js", 
    "scripts": {
      "start": "node server.js",
      "build": "mkdir -p publicttb && (test -d publicttb && echo 'Directory already exists' || (cp index.html publicttb && cp styles.css publicttb && cp server.js publicttb))"

    },
    
    "dependencies": {
      "express": "^4.17.1"
    }
  }
  